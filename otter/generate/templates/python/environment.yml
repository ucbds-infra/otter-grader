{% if not other_environment %}name: {{ otter_env_name }}
channels:
    - defaults
    - conda-forge
dependencies:
    - python=3.7
    - pip
    - nb_conda_kernels
    - gcc_linux-64
    - gxx_linux-64
    - pip:
        - -r requirements.txt{% else %}{{ other_environment }}{% endif %}