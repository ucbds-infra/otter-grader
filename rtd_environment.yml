name: otter-grader
channels: 
    - conda-forge
    - defaults
dependencies:
    - python>=3.6
    - pip
    - pip:
        - -r requirements.txt
        - -r docs/requirements.txt
        - nbconvert
        - ipython
